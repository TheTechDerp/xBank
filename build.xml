<project name="simpleCompile" default="deploy" basedir=".">
    <target name="init">
        <property name="sourceDir" value="src" />
        <property name="outputDir" value="bin" />
        <property name="libdir" value="../lib/" />
        <property name="jarname" value="XBank" />
    </target>
    <target name="clean" depends="init">
        <delete dir="${outputDir}" />
    </target>
    <target name="prepare" depends="clean">
        <mkdir dir="${outputDir}" />
    </target>
    <target name="compile" depends="prepare">
        <javac destdir="${outputDir}">
            <src path="${sourceDir}"/>
            <classpath>
                <fileset dir="${libdir}">
                    <include name="*.jar" />
                </fileset>
            </classpath>
        </javac>
    </target>
    <target name="deploy" depends="compile,init">
          <zip destfile="../out/${jarname}.jar">
            <fileset dir="${outputDir}"/>
            <fileset dir="${sourceDir}" includes="plugin.yml"/>

          </zip>
    	
    </target>
</project>